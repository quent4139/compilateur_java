#include "Math.decah"

{
	int cpt = 0;
	float x = 0.000001;
	float pas = 10;
	float res;
	float res2;
	Math m = new Math();
	while(cpt < 100) {
		res = m.atan(x);
		res2 = m.atan(1/x);
		print("atan(", x ,") + atan(", 1.0/x ,") = ", res + res2, "    ");
		if(((cpt + 1) % 3)==0) {
			println("");
		}
		x = x + pas;
		cpt = cpt + 1;
	}
}